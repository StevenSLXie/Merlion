# Ralph Codex Progress Log
Started: Sat Apr 25 18:33:07 +08 2026
## Codebase Patterns
- Keep request-local overlays visible in `runLoop()` return state for the active submit, but prune them before storing `QueryEngine` history or resume state.
- Keep sticky capability-profile epochs stable on lightweight follow-up prompts; only rotate schema when the user makes an explicit correction or phase switch rather than inferring it from a task-kind change alone.
- Emit per-turn observability change reasons only on the provider turn where the schema epoch actually changes; tool-follow-up turns in the same submit should keep the schema hash but clear the change reason.
---
## [2026-04-25 19:21:16 +0800] - SPEC-108-real-request-prompt-observability
- What was implemented
- Independently reviewed real-request prompt observability, found a per-turn schema-change attribution bug on tool-follow-up turns, and fixed the loop so schema change reasons are only emitted on the turn where the epoch actually changes.
- Files changed
- `src/runtime/loop.ts`
- `tests/query_engine.test.ts`
- `scripts/ralph-codex/prd.json`
- `scripts/ralph-codex/progress.txt`
- `scripts/ralph-codex/current_story.json` (removed)
- Verification run
- `node --experimental-strip-types --test tests/query_engine.test.ts`
- `node --experimental-strip-types --test tests/prompt_observability.test.ts`
- `node --experimental-strip-types --test tests/session.test.ts`
- Review notes:
- Checked `docs/features/108-prefix-stability-first-context-assembly.md`, the handoff story, and the observability wiring in `src/runtime/prompt_observability.ts`, `src/runtime/query_engine.ts`, `src/runtime/loop.ts`, `src/runtime/runner.ts`, and `src/runtime/session.ts`.
- Found one P1 issue: `runLoop()` reused the epoch-level `schema_change_reason` for every provider turn inside the same `submitPrompt()` run, so tool-follow-up turns incorrectly looked like repeated schema changes even when the real request shape and tool schema were unchanged.
- Resolved it by scoping `schema_change_reason` to the first provider turn of the submit, then adding a regression that proves tool-follow-up usage entries keep the same schema hash while clearing the change reason back to `null`.
- Learnings for future iterations:
- Reusable patterns
- Per-turn observability fields need turn-boundary semantics, not submit-boundary semantics; otherwise multi-turn tool continuations will silently over-report state changes.
- Gotchas
- A single `submitPrompt()` can emit multiple persisted usage rows, so review observability fields against tool-call continuations, not just one-request-per-row scenarios.
- Useful context
- `PromptObservabilityTracker.record()` already computes hashes from the actual assembled request each turn, so the fix belongs at the loop callsite that decides when a schema transition should be attributed.
---
## [2026-04-25 19:16:57 +0800] - SPEC-108-prompt-layering-non-persistent-overlay
- What was implemented
- Independently reviewed the new prompt layering contract, then fixed a follow-up regression so request-local overlays remain visible in the active `runLoop()` result while still staying out of persisted transcript history and resume state.
- Files changed
- `src/runtime/loop.ts`
- `scripts/ralph-codex/prd.json`
- `scripts/ralph-codex/progress.txt`
- `scripts/ralph-codex/current_story.json` (removed)
- Verification run
- `node --experimental-strip-types --test tests/runtime_loop.test.ts`
- `node --experimental-strip-types --test tests/query_engine.test.ts`
- `node --experimental-strip-types --test tests/e2e/e2e_query_engine_local.test.ts`
- `node --experimental-strip-types --test tests/e2e/e2e_subagent_local.test.ts`
- Review notes:
- Checked `docs/features/108-prefix-stability-first-context-assembly.md`, the handoff story, and the runtime changes in `src/runtime/query_engine.ts`, `src/runtime/loop.ts`, `src/runtime/items.ts`, and `src/runtime/subagents.ts`.
- Found one P1 issue: the new overlay split removed runtime overlays from `RunLoopResult.state.items`, which broke the “visible for the full submit” contract and regressed existing `tests/runtime_loop.test.ts` guardrail coverage even though story-specific tests were green.
- Resolved it by projecting overlays back into the returned loop state while keeping `QueryEngine` persistence on the pruned item set, so active-run visibility and non-persistent history semantics both hold.
- Learnings for future iterations:
- Reusable patterns
- When runtime state is split into persistent and non-persistent lanes, keep a separate review check for the returned in-memory state surface; persistence fixes can still regress active-run observability.
- Gotchas
- Story-focused tests can miss regressions in shared loop-state contracts, so run adjacent runtime suites when changing `runLoop()` state projection or item assembly.
- Useful context
- `QueryEngine.submitPrompt()` already prunes non-persistent runtime items before updating stored history, so `runLoop()` can expose overlays in its return value without leaking them into resume state.
---
## [2026-04-25 19:08:49 +0800] - SPEC-108-sticky-tool-profile-epochs
- What was implemented
- Independently reviewed the sticky capability-profile epoch work, found an implicit schema-churn regression on lightweight implementation follow-ups, and tightened the phase-switch heuristic so sticky schemas only rotate on explicit boundaries.
- Files changed
- `src/runtime/task_state.ts`
- `tests/task_state.test.ts`
- `tests/query_engine.test.ts`
- `scripts/ralph-codex/prd.json`
- `scripts/ralph-codex/progress.txt`
- `scripts/ralph-codex/current_story.json` (removed)
- Verification run
- `node --experimental-strip-types --test tests/task_state.test.ts`
- `node --experimental-strip-types --test tests/query_engine.test.ts`
- `node --experimental-strip-types --test tests/tool_pool.test.ts`
- `node --experimental-strip-types --test tests/runtime_loop.test.ts`
- `node --experimental-strip-types --test tests/e2e/helpers_registry.test.ts`
- `node --experimental-strip-types --test tests/e2e/e2e_task_runtime_local.test.ts`
- `node --experimental-strip-types --test tests/e2e/e2e_session_resume.test.ts` (skipped because `OPENROUTER_API_KEY` is not set)
- Review notes:
- Checked `docs/features/108-prefix-stability-first-context-assembly.md`, the handoff story, and the sticky-epoch flow in `src/runtime/query_engine.ts`, `src/runtime/task_state.ts`, `src/runtime/state/types.ts`, `src/runtime/runner.ts`, and `src/tools/pool.ts`.
- Found one P1 issue: `resolveCapabilityProfileEpoch()` treated any task-kind change involving `implementation`, `review`, `verification`, or `meta_correction` as an automatic phase switch, so a lightweight follow-up like “What else should I inspect next?” silently dropped the active implementation schema even without an explicit boundary.
- Resolved it by requiring explicit phase-switch phrasing for non-correction schema changes and adding unit/integration regressions that prove implementation follow-ups keep the prior schema hash while task control still updates for the new prompt.
- Learnings for future iterations:
- Reusable patterns
- Sticky schema logic should bias toward keeping the prior epoch and letting task-control or mutation-policy guards absorb lightweight prompt drift.
- Gotchas
- Task-kind classifiers are too coarse to drive schema rotation on their own; using them directly causes prompt-shape churn that contradicts stable-prefix goals.
- Useful context
- The review regression is easiest to surface in `tests/query_engine.test.ts` by comparing provider-visible tool names across consecutive turns, not by inspecting runtime state alone.
---
